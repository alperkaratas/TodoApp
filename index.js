/**
 * @format
 */

import {AppRegistry} from 'react-native';
import todoMain from './src/todoMain';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => todoMain);
